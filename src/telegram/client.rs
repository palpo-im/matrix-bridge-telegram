pub struct TelegramClientWrapper;

impl TelegramClientWrapper {
    pub fn new() -> Self {
        Self
    }
}

impl Default for TelegramClientWrapper {
    fn default() -> Self {
        Self::new()
    }
}
